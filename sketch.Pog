function setup() {
  createCanvas(400, 400);
}

function draw() {
  background(220);
}
function setup() {
  createCanvas(400, 400);
  background("pink")

  textFont("Comic Sans MS");
  textSize(30)
  textAlign(CENTER,CENTER);

}

function draw(){
  text("poggers dms",200,200)
}
